import styled from 'styled-components'

// common styles
export const ForecastPageContainer = styled.div`
  position: relative;
  margin: 0 auto;
  clear: left;
  height: auto;
  z-index: 0;
  text-align: center;
  margin-top: 50px;
`
